import { maxYear, minYear } from "../../helpers/magic-numbers";

const initialOption = "Popularity";
const initialYear: number[] | number = [minYear, maxYear];

export { initialOption, initialYear };
